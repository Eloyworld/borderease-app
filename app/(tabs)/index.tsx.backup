# Create a backup of the current file
Copy-Item -Path "app/(tabs)/index.tsx" -Destination "app/(tabs)/index.tsx.backup"

# Update the file with improved logo styling
@"
import { Image, StyleSheet, Text, View } from 'react-native';
import logo from '../../assets/images/icon.png';

export default function HomeScreen() {
  return (
    <View style={styles.container}>
      <Image source={logo} style={styles.logo} />
      <Text style={styles.title}>Beyond Borders</Text>
      <Text style={styles.subtitle}>Your travel companion for border crossings</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f8f9fa',
    padding: 20,
  },
  logo: {
    width: '80%',
    height: undefined,
    aspectRatio: 1, // Maintains square aspect ratio
    marginBottom: 30,
    resizeMode: 'contain',
  },
  title: {
    fontSize: 36,
    fontWeight: 'bold',
    color: '#2c3e50',
    marginBottom: 12,
    textAlign: 'center',
  },
  subtitle: {
    fontSize: 20,
    color: '#7f8c8d',
    textAlign: 'center',
    maxWidth: '80%',
  },
});
"@ | Out-File -FilePath "app/(tabs)/index.tsx" -Encoding utf8